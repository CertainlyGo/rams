model_name: "distilbert-base-uncased"
max_length: 128
batch_size: 8
lr: 3.0e-5
epochs: 2
weight_decay: 0.01
warmup_ratio: 0.1
eval_every_steps: 20
synthesis_multiplier: 1
alpha: 0.25
beta: 0.35
gamma: 0.40
dropout_passes: 3
output_root: "outputs"

